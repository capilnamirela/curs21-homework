import { Routes } from '@angular/router';
import { SalaryComponent } from './salary/salary.component';

export const routes: Routes = [
{path:'angajati/datePersonale', component:SalaryComponent}, {path:'angajati/dateAngajareUnitate', component:SalaryComponent}
    ];
